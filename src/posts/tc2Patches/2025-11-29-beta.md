---
date: 2025-11-29
---

* Added `tf_beta_mode 1` ConVar to enable beta features
    * Will add the `beta` tag to the server
* Phlogistinator MMMPH! gain now scales down to a minimum of 50% over distance (256->1024 units)
* CTF flag carriers on the enemy team now have an outline showing their location
* Flag return timer will no longer reset upon pickup
* Flag return timer increments back up to full while picked up
    * Scouts increment the return timer twice as fast
* Capturing the flag no longer grants crits to the capturing team
